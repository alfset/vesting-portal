import type { AccountImportData } from "@near-wallet-selector/core";
interface DecryptAccountDataProps {
    ciphertext: string;
    secretKey: string;
}
interface EncryptAccountDataProps {
    accountData: Array<AccountImportData>;
    secretKey: string;
}
export declare const encryptAccountData: ({ accountData, secretKey, }: EncryptAccountDataProps) => string;
export declare const decryptAccountData: ({ ciphertext, secretKey, }: DecryptAccountDataProps) => Array<AccountImportData>;
export declare const generateSecretKey: () => string;
export {};

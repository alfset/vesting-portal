import type { InjectedWallet, Network, WalletModuleFactory } from "@near-wallet-selector/core";
import type { InjectedXDEFI, NearXDEFI } from "./injected-xdefi";
declare global {
    interface Window {
        xfi: InjectedXDEFI | undefined;
    }
}
export interface XDEFIState {
    wallet: NearXDEFI;
    network: Network;
}
export interface XDEFIWalletParams {
    iconUrl?: string;
    deprecated?: boolean;
}
export declare function setupXDEFI({ iconUrl, deprecated, }?: XDEFIWalletParams): WalletModuleFactory<InjectedWallet>;

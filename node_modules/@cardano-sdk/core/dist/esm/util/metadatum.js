import { CML } from '../CML/CML';
import { txMetadatum as txMetadatumToCML } from '../CML/coreToCml/coreToCml';
import { txMetadatum as txMetadatumToCore } from '../CML/cmlToCore/cmlToCore';
import { usingAutoFree } from '@cardano-sdk/util';
export const asMetadatumMap = (metadatum) => {
    if (metadatum instanceof Map) {
        return metadatum;
    }
    return null;
};
export const asMetadatumArray = (metadatum) => {
    if (Array.isArray(metadatum)) {
        return metadatum;
    }
    return null;
};
export const jsonToMetadatum = (json) => txMetadatumToCore(CML.encode_json_str_to_metadatum(JSON.stringify(json), CML.MetadataJsonSchema.NoConversions));
export const metadatumToJson = (metadatum) => usingAutoFree((scope) => JSON.parse(CML.decode_metadatum_to_json_str(txMetadatumToCML(scope, metadatum), CML.MetadataJsonSchema.NoConversions)));
//# sourceMappingURL=metadatum.js.map
import { HexBlob, bufferToHexString, usingAutoFree } from '@cardano-sdk/util';
import { cmlUtil, coreToCml } from '../CML';
export const TxCBOR = (tx) => HexBlob(tx);
TxCBOR.serialize = (tx) => bufferToHexString(Buffer.from(usingAutoFree((scope) => coreToCml.tx(scope, tx).to_bytes())));
TxCBOR.deserialize = (tx) => cmlUtil.deserializeTx(tx);
//# sourceMappingURL=TxCBOR.js.map
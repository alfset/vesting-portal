import { CML } from '../CML/CML';
import { HexBlob, usingAutoFree } from '@cardano-sdk/util';
export const TxBodyCBOR = (tx) => HexBlob(tx);
TxBodyCBOR.fromTxCBOR = (txCbor) => Buffer.from(usingAutoFree((scope) => scope.manage(scope.manage(CML.Transaction.from_bytes(Buffer.from(txCbor, 'hex'))).body()).to_bytes())).toString('hex');
//# sourceMappingURL=TxBodyCBOR.js.map
import { CardanoNodeErrors } from '../../CardanoNode';
export const isTxSubmissionError = (error) => Object.values(CardanoNodeErrors.TxSubmissionErrors).some((TxSubmitError) => error instanceof TxSubmitError);
export const asTxSubmissionError = (error) => {
    if (Array.isArray(error)) {
        for (const err of error) {
            if (isTxSubmissionError(err)) {
                return err;
            }
        }
        return null;
    }
    return isTxSubmissionError(error) ? error : null;
};
//# sourceMappingURL=txSubmissionErrors.js.map
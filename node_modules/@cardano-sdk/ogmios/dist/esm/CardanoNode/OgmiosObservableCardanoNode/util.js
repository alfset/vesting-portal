import { Cardano } from '@cardano-sdk/core';
export const pointOrOriginToOgmios = (point) => point === 'origin'
    ? 'origin'
    : {
        hash: point.hash,
        slot: point.slot
    };
export const ogmiosToCorePoint = (point) => ({
    hash: Cardano.BlockId(point.hash),
    slot: Cardano.Slot(point.slot)
});
export const ogmiosToCoreTip = (tip) => ({
    ...ogmiosToCorePoint(tip),
    blockNo: Cardano.BlockNo(tip.blockNo)
});
export const ogmiosToCoreTipOrOrigin = (tip) => (tip === 'origin' ? tip : ogmiosToCoreTip(tip));
export const ogmiosToCorePointOrOrigin = (point) => point === 'origin' ? point : ogmiosToCorePoint(point);
//# sourceMappingURL=util.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.genesisConfig = void 0;
const errors_1 = require("../../errors");
const Query_1 = require("../Query");
const isEraMismatch = (result) => result.eraMismatch !== undefined;
const isGenesisConfig = (result) => result.systemStart !== undefined;
const genesisConfig = (context) => (0, Query_1.Query)({
    methodName: 'Query',
    args: {
        query: 'genesisConfig'
    }
}, {
    handler: (response, resolve, reject) => {
        if (response.result === 'QueryUnavailableInCurrentEra') {
            return reject(new errors_1.QueryUnavailableInCurrentEraError('genesisConfig'));
        }
        else if (isGenesisConfig(response.result)) {
            return resolve(response.result);
        }
        else if (isEraMismatch(response.result)) {
            const { eraMismatch } = response.result;
            const { ledgerEra, queryEra } = eraMismatch;
            return reject(new errors_1.EraMismatchError(queryEra, ledgerEra));
        }
        else {
            return reject(new errors_1.UnknownResultError(response.result));
        }
    }
}, context);
exports.genesisConfig = genesisConfig;
//# sourceMappingURL=genesisConfig.js.map
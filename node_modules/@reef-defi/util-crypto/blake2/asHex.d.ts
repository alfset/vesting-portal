import type { HexString } from '@reef-defi/util/types';
/**
 * @name blake2AsHex
 * @summary Creates a blake2b hex from the input.
 * @description
 * From a `Uint8Array` input, create the blake2b and return the result as a hex string with the specified `bitLength`.
 * @example
 * <BR>
 *
 * ```javascript
 * import { blake2AsHex } from '@reef-defi/util-crypto';
 *
 * blake2AsHex('abc'); // => 0xba80a53f981c4d0d
 * ```
 */
export declare function blake2AsHex(data: HexString | Uint8Array | string, bitLength?: number, key?: Uint8Array | null, onlyJs?: boolean): HexString;
